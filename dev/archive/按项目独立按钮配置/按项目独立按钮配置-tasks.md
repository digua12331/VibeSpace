# 按项目独立按钮配置 · 任务清单

- [x] 步骤 1：改造 `packages/web/src/customButtons.ts` —— per-project key + cache map + listener 协议变更 + `migrateGlobalToPerProject` → verify: 文件改完跑 `pnpm --filter @aimon/web build` 通过（含 tsc，三处调用方的旧签名 type error 会暴露）。**已通过**：build ✓ in 1.61s，无 type error。
- [x] 步骤 2：改 `packages/web/src/components/PermissionsDrawer.tsx` —— `ButtonsTab` 接入 `useStore selectedProjectId/projects`，传 projectId 给新 API，加 `selectedProjectId===null` 占位 + 禁用编辑，加 add/update/remove 时 `pushLog` → verify: build 通过；浏览器手测设置抽屉打开后能看到当前项目按钮，切项目时列表跟着变。**已通过**：build ✓；实现细节调整——`PermissionsDrawer` 本来就接 `project: Project` prop，直接把它传给 `ButtonsTab`，比读 `selectedProjectId` 更准确（抽屉永远是项目级，不会出现 null）。占位逻辑省掉。浏览器手测：tester 工具不可用，留给大哥验。
- [x] 步骤 3：改 `packages/web/src/components/terminal/SessionView.tsx` —— `useState(getCustomButtons(session.projectId))` + listener 只在 `projectId === session.projectId` 时更新；依赖数组加 `session.projectId`；`session.projectId` 不在 `projects` 列表时返回 `[]` 但不写入 → verify: build 通过；浏览器手测同时开两个不同项目的 session 各自按钮独立。**已通过**：build ✓；浏览器手测留给大哥。
- [x] 步骤 4：改 `packages/web/src/App.tsx` —— useEffect 监听 `useStore(s=>s.projects)`，首次非空时调一次 `migrateGlobalToPerProject(ids)`，成功时 `pushLog` 一条 `custom-buttons-migrated` → verify: 清空 localStorage + 手动塞旧 array → 刷新 → 所有项目都注入了原 6 个按钮 + LogsView 看到迁移日志；再刷一次不重复迁移（防重标记生效）。**已通过**：build ✓；运行时迁移效果留给大哥用真实 localStorage 状态验证（大哥本机的旧 `aimon_custom_buttons_v1` 应该已存在，刷新页面后会自动迁移）。
- [x] 步骤 5：grep + 白名单 + tester → verify: (a) `git diff --name-only HEAD` 输出只在 plan 写的 4 个文件白名单内；(b) grep `getCustomButtons|setCustomButtons|onCustomButtonsChange` 全仓库无残留旧签名；(c) 派 `vibespace-browser-tester` 跑 plan 验收清单。**部分通过**：(a) git diff 9 个文件，其中 5 个是任务开始前就 modified 的别任务草稿（顶部 gitStatus 列过），本任务实际只动 4 个白名单文件 ✓；(b) grep 全部命中都带 projectId 参数，无残留旧签名 ✓；(c) **tester 工具 (browser-use MCP) 当前会话未注册，全部 SKIP**——这是 MCP 基础设施问题，不在 AI 能解决范围内，需大哥手动验收。
