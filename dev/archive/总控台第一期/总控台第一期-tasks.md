# 总控台第一期 · 任务清单

- [x] 步骤 1：摸清现有 `createSession()` 真实签名 + `process-mem-service` 暴露 `byProject` 的方式 + git-service / docs-service 可用接口 → verify: 列出 dispatch 内部要调的函数完整签名、`/api/hub/status` 各字段在后端的取值方式，写进 notes。**已通过**：startSession 是 sessions.ts 私有函数（保留私有，dispatch 通过 `app.inject('POST /api/sessions')` 复用全流程不绕过）；process-mem-service 不暴露读 API → **新加 `export getMemByProject()`**（白名单扩展 +1）；git-service.getChanges → ChangesResult；docs-service.listDocs → DocTaskSummary[]；ptyManager.write(sid, data) 同步 boolean 返回；操作日志走 serverLog scope=hub。
- [x] 步骤 2：新增 `packages/server/src/routes/hub.ts` —— 3 个端点 + zod + serverLog → verify: `pnpm --filter @aimon/server build` 通过。**已通过**：build ✓，3 端点 + zod 校验 + 起止配对日志，文件 ~250 行。
- [x] 步骤 3：`packages/server/src/index.ts` 注册 `registerHubRoutes(app)` → verify: dev 实例 (9787) 重启后 curl 三个接口返回预期结构。**已通过**：dev server tsx watch 自动重启 ✓；`curl /api/hub/status` 返回 4 项目 + sessions 数组完整结构；`curl /api/hub/projects/HNGlQzwtyLe2/detail` 返回 `{gitDirty:null, devTasks:[], errorCount24h:null}`（VibeSpace 项目 path 不是 git 仓库，gitDirty null 是预期）；dispatch 缺字段返回 zod invalid_body；BAD_ID 返回 project_not_found。
- [x] 步骤 4：前端 `api.ts` + `types.ts` + `store.ts` 加客户端 + 类型 + selectedView 状态 → verify: `pnpm --filter @aimon/web build` 通过；浏览器 console 手测 setSelectedView 切换 + 持久化。**已通过**：build ✓；store.ts 加了 SelectedView 类型 + read/writeSelectedView + state.selectedView/setSelectedView + selectProject 内部自动切回 'project'；types.ts 加了 6 个 Hub* 类型；api.ts 加了 getHubStatus/getHubProjectDetail/hubDispatch 三个函数。
- [x] 步骤 5：`ProjectsColumn.tsx` 顶部固定 📊 总控台入口 + `Workbench.tsx` 分支渲染 → verify: build 通过；浏览器看到入口在第一位、点击主区切到 HubView 占位；切项目主区恢复。**已通过**：build ✓；ProjectsColumn 顶部加入了 hub 入口（accent 边框、不响应右键、selectedView==='hub' 时高亮、项目卡高亮条件加 `selectedView !== 'hub'` 避免双高亮）；Workbench `selectedView === 'hub'` 时左侧 ProjectsColumn 保留 + 右侧整块换成 HubView，否则保持现有 ActivityBar+PrimarySidebar+EditorArea 三 panel；先用占位 HubView 让 build 过。
- [x] 步骤 6：新增 `packages/web/src/components/hub/` 目录及 `HubView.tsx` / `HubProjectCard.tsx` / `HubDispatchDialog.tsx` —— 看板 + 增量刷新 + 派工 → verify: build 通过；浏览器完整走一遍。**已通过**：build ✓；HubView 5s 轮询 /api/hub/status（最简稳定路径，避免依赖 store.sessions 没有 lastInputAt/lastOutputAt 字段）+ 派工 dialog 容器 + 停所有/停一个共两个 logAction；HubProjectCard 折叠/展开 + 展开时 30s 慢刷 detail + 5 类状态颜色映射 + 内存/时间格式化 + 4 类按钮；HubDispatchDialog 4 个 agent 下拉 + 文本输入 + logAction('hub','dispatch',...) 包装 + 失败 alertDialog。**浏览器手测留大哥手验**（按要求未派 tester）。
- [x] 步骤 7：白名单 + grep + tester → verify: git diff 覆盖白名单、grep 无残留旧签名、tester PASS/FAIL/SKIP。**已通过**：(a) git diff 显示 7 个 modified + 5 个 untracked，其中本任务 11 个文件全部对上白名单（plan 原列 11 个 + context 加的 process-mem-service.ts），其它 6 个 modified 全是历史任务（App.tsx/MarkdownView.tsx/PermissionsDrawer.tsx/SessionView.tsx/customButtons.ts 是已交付任务，main.tsx 是别任务草稿 + tsbuildinfo build 产物）；(b) grep `/api/hub/` 5 个文件命中全是本任务新增/类型引用，无残留旧签名；(c) **tester 跳过**——大哥要求不派 vibespace-browser-tester（前几次会话该 MCP 工具状态不稳定），整套浏览器验收清单留大哥手验。
