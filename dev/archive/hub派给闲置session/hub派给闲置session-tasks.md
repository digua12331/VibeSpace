# hub 派给闲置 session · 任务清单

- [x] 步骤 1：后端 statusManager 加 claimIdle / releaseIdleClaim + statusChangedAt + dispatchLocks → **已通过**：build ✓；ClaimIdleResult 类型 export；statusChangedAt Map + dispatchLocks Set 模块级；set() 内部更新 statusChangedAt + status==='idle' 时自动清 lock；claimIdle 原子检查 + 设 working + 加 lock + 返结构化 code；releaseIdleClaim 仅清 lock 不改 status。
- [x] 步骤 2：pty-manager + ws-hub → **已通过（零改动 - 现成）**：发现 pty-manager.ts 早已 export `lastInputAt: Map<string, number>` + `lastOutputAt`，ws-hub.ts 收 client `input` 时已经在 `lastInputAt.set(sid, Date.now())` —— **跳过新增 markUserInput / markProgrammaticInput / getLastInputAt 函数**，端点直接 import lastInputAt 用 Map .get/.set 即可。比 plan 估算 +20 行变 0 行。
- [x] 步骤 3：后端 POST /api/hub/dispatch-to-idle-session 6 步流程 + 错误码体系 → **已通过**：build ✓；端点完整实现（sanitize text + getSession + agent claude + ptyManager.has + waiting_input check + lastInputAt window + claimIdle 抢占 + PTY write + 失败 releaseIdleClaim 回滚 + 防下次立刻派 lastInputAt.set）；3 个核心拒绝 curl 验过：404 session_not_found ✓、400 invalid_body ✓、400 not_ai_session（shell）✓；剩余 claim 系列拒绝（waiting_input/recently_typed/not_idle/idle_too_fresh/locked）需要真 claude session 才能触发，逻辑通过 TS 编译，留浏览器手验。
- [x] 步骤 4：MCP server 加 dispatch_to_idle_session 工具 → **已通过**：build ✓；tools/list 返 **8 工具** ✓ 含新增 dispatch_to_idle_session；描述明说 7 个前置条件 + 失败建议 hub claude 改用 dispatch_to_project 新建。
- [x] 步骤 5：前端 types + api + HubDispatchDialog 改造单选 → **已通过**：build ✓ 1.55s；types 加 DispatchToIdleSession Request/Response；api 加 dispatchToIdleSession；HubDispatchDialog 大改：[● 新建 session  ○ 派给已有空闲 claude 终端] 单选默认新建；候选下拉 filter (projectId + agent='claude' + liveStatus='idle' + ended_at==null)；无候选时单选项变灰禁用；失败 alertDialog 显示 error.code 给大哥看。
- [x] 步骤 6：白名单 + grep + tester → **已通过**：(a) git diff 列 6 modified + 10 untracked，本任务实际改 **6 文件**全在白名单（pty-manager.ts 和 ws-hub.ts 因 step 2 发现现成跳过，不算越界）；(b) grep `claimIdle|releaseIdleClaim|dispatch_to_idle_session|dispatch-to-idle-session` 4 文件命中，新符号散布合理（status.ts 定义 + routes/hub.ts 调用 + mcp-hub 调用 + api.ts 调用）；markUserInput/markProgrammaticInput 无命中（未新增函数）；(c) tester 按既定不派，留大哥手验。
