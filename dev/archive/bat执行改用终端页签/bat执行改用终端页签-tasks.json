{
  "task": "bat执行改用终端页签",
  "steps": [
    {
      "id": 1,
      "title": "改 fileContextMenu.ts 的执行条目：createSession('cmd') → addSession/setActiveSession/setActiveTabKind → subscribe → 120ms 后 sendInput(cd /d dir && file.bat\\r)",
      "verify": "grep execBatFile 无匹配；能看到 createSession({ projectId, agent: 'cmd' }) 和 aimonWS.sendInput(",
      "status": "done"
    },
    {
      "id": 2,
      "title": "删 api.ts 里 execBatFile 整段导出",
      "verify": "grep -rn execBatFile packages/web/src 为空",
      "status": "done"
    },
    {
      "id": 3,
      "title": "删 fs-ops.ts 里 /fs/exec-bat 路由整块（spawn import 因 reveal/open-vscode 仍在用故保留）",
      "verify": "grep -rn exec-bat packages/server/src 为空",
      "status": "done"
    },
    {
      "id": 4,
      "title": "项目层面类型检查：pnpm -r build",
      "verify": "退出码 0，无 TS 报错",
      "status": "done"
    },
    {
      "id": 5,
      "title": "浏览器冒烟：pnpm dev:alt → 9788 → 右键 start.bat → 执行",
      "verify": "不弹独立 cmd 窗口；UI 出现新 cmd 页签并聚焦；页签里看到 VibeSpace 横幅输出",
      "status": "doing"
    }
  ]
}
