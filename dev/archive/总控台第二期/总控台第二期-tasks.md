# 总控台第二期 · 任务清单

- [x] 步骤 1：调研 + 摸 MCP SDK 落地形态 → verify: SDK 装好 + 列骨架。**已通过**：`@modelcontextprotocol/sdk@1.29.0` 安装成功（48.9s）；SDK 真实 API = `McpServer + StdioServerTransport + registerTool(name, {description, inputSchema (zod)}, async handler)`，骨架明确。
- [x] 步骤 2：后端 hub workspace + token + 鉴权 → verify: build + curl。**已通过**：build ✓；hub workspace 创建（含 README.md）；鉴权 hook 第一版漏洞修了（带错 token 时不能 fallback loopback 放行）；最终 curl 验证：无 token + loopback → 200；wrong token → 401；正确 token → 200。
- [x] 步骤 3：后端 hub session runtime + 路由 → verify: POST 起 + WS 拿输出 + DELETE 清。**已通过**：build ✓；`POST /api/hub/session {agent:"shell"}` 返回 `{hubId, pid, ptySessionId:"hub:<id>", workspaceDir, startedAt}`；ws 订阅 hub:<id> 即时拿到输出；`DELETE /api/hub/session/<id>` 返 204；listHubSessions 之后空数组（runtime 清理 ✓）；LogsView 看到 scope=hub session-start/stop 配对。
- [x] 步骤 4：后端 path-guard + hub.ts 加 git-log/file 2 端点 → verify: build + 攻击向量测试。**已通过**：build ✓；ExcelCon git-log 返 commit 实数据 ✓；`?path=../../etc/passwd` → 400 `path_escape` ✓；`?path=/etc/passwd`（absolute）→ 400 `path_escape` ✓；不存在文件 → 404 `not_found` ✓。file_too_large / binary_file 走相同 readGuarded 路径（逻辑上验证，未造大文件实测）。
- [x] 步骤 5：MCP server 单文件 bin + package.json bin entry → verify: build + stdio JSON-RPC handshake。**已通过**：build ✓；`dist/mcp-hub/index.js` 存在 ✓；stdio handshake `initialize` 返 `aimon-mcp-hub v0.1.0`；`tools/list` 返 6 工具完整 schema；`tools/call hub_status` 在 token 同步后返 `{server_ok:true, projects_count:4}`；`tools/call read_file` 攻击向量 → 结构化错误 `{code:"path_escape", retryable:false}`。
- [x] 步骤 6：hub-workspace .mcp.json 真实生成 → verify: cat .mcp.json 内容完整。**已通过**：跟 step 3 合并完成——hub-session-runtime 启动时即调 writeHubMcpConfig 重写；cat 输出包含 `command: <node 解释器绝对路径>`、`args: [mcp-hub bin 绝对路径]`、`env.HUB_TOKEN` + `env.AIMON_BACKEND_PORT`。
- [x] 步骤 7：前端 api + types + store → verify: build + console。**已通过**：build ✓；types 加 6 个 Hub* 类型（含 HubSessionInfo / StartHubSessionRequest）；api 加 startHubSession / stopHubSession / listHubSessions；store 加 currentHubSession + setter，sessionStorage 持久化（不到 localStorage，对齐 PTY 不跨浏览器会话存活的语义）。
- [x] 步骤 8：HubTerminal 简化版组件 → verify: build + 渲染。**已通过**：build ✓ in 1.55s；HubTerminal ~150 行（远低于 plan 估的 400-500，省了 task/worktree/permission/customButtons 等全部项目级耦合）；用 xterm + FitAddon + WebLinks + WebGL fallback；订阅 aimonWS hub:<id>；onData → sendInput；ResizeObserver 自适应；主题响应。**未做** IME 守卫 / 自定义键盘透传（disableStdin: false 直接 onData 已经够用），如大哥实测体验明显劣化第 2.5 期迭代。
- [x] 步骤 9：HubView 接入 + 启动/停止 UI + agent 下拉 → verify: build + 端到端浏览器走一遍。**已通过**：build ✓；HubView 改造为左右两栏（左看板 + 右 hub session 区，固定宽 480px）；右栏未启动时显示 HubStartPanel 占位（agent 下拉 + 启动按钮），已启动时渲染 HubTerminal + 顶栏停止按钮；onStartHubSession / onStopHubSession 都用 logAction 包；reconcile effect：store 有但 server 没有自动清；启动/停止失败弹 alertDialog。**端到端浏览器走一遍留大哥手验**（按既定不派 tester）。
- [x] 步骤 10：安全 + 白名单 + grep + tester → verify: 攻击向量 + git diff + HUB_TOKEN 泄漏 + tester。**已通过**：(a) 攻击向量在 step 4+5 已实测拒绝（path_escape / absolute path / 不存在）；(b) git diff 列 4 modified + 8 untracked，本任务 14 白名单文件全覆盖 + pnpm-lock.yaml 合理副作用（MCP SDK），其它 9 个 modified/untracked 都是历史任务已交付；(c) grep HUB_TOKEN 命中全是 .mcp.json / README / 源代码 import 注释合理引用，**无 serverLog/pushLog 参数泄漏**；(d) **tester 跳过**——按大哥既定要求不派 vibespace-browser-tester，端到端浏览器验收清单留大哥手验。
