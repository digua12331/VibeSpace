# 面板加MCP与项目级开关 · 任务清单

- [x] 1. 新建 `claude-json-service.ts`：读写 `~/.claude.json` 项目段 disabledMcpServers，原子写 + 大小写兜底路径键匹配 → verify: 临时跑一段 Node 调用 `getDisabledMcpServersForProject` / `setDisabledMcpServersForProject`，写入后 `~/.claude.json.projects.<key>.disabledMcpServers` 出现，其它字段不变
- [x] 2. 扩 `claude-settings.ts`：加 scope 参数支持项目级 `<proj>/.claude/settings.json` 读写；现有 `readClaudeSettings()` / `patchClaudeSettings(patch)` 不带 scope 时默认全局，向后兼容 → verify: 现有 `GET /api/claude-settings` 仍返回全局；新调用 `readClaudeSettings({kind:'project', projectPath})` 能读到项目级 settings
- [x] 3. 新路由 `routes/project-claude-settings.ts`：`GET/PUT /api/project-claude-settings?projectId` + 注册到 `index.ts`；起止日志 scope=`project-claude-settings` → verify: curl GET 返回 `{enabledPlugins, skillOverrides, path, exists}`；PUT 改后 GET 能看到；非法 projectId 返 400/500 + LogsView 出现 ERROR 条目
- [x] 4. 改 `mcp-bridge.ts`：抽 `removeFromMcpJson(projectPath, mcpName)` export；`injectClaude` 写前 check 项目级 disabledMcpServers，含 MCP_KEY 则 skip + 调 removeFromMcpJson 清残留；起止日志 scope=`installer` action=`skip-mcp-browseruse` 或 `inject-mcp-browseruse`（保持现有行为名） → verify: 把 browser-use 加进 disabledMcpServers，重启 Claude 会话，`.mcp.json` browser-use 条目消失，LogsView 看到 `installer skip-mcp-browseruse` 起止配对
- [x] 5. 新路由 `routes/mcp-servers.ts`：`GET /api/mcp-servers?projectId` 汇总全局 + 项目 MCP；`PUT /api/mcp-servers/toggle` 写 disabledMcpServers + 调 removeFromMcpJson；注册到 `index.ts`；起止日志 scope=`mcp-toggle` → verify: curl GET 看到 browser-use + codegraph + enabled 状态；PUT toggle 关 browser-use 后 `~/.claude.json` 项目段出现 disabledMcpServers + `.mcp.json` browser-use 被移除；故意传非法 mcp name 返 400 + ERROR 日志
- [x] 6. 前端 types + api：`types.ts` 加 `ProjectClaudeSettings` `McpServerEntry` `McpServerListResult` `PluginOverrideState`；`api.ts` 加 `getProjectClaudeSettings` `patchProjectClaudeSettings` `listMcpServers` `toggleMcpServer` → verify: `pnpm --filter @aimon/web type-check`（或 tsc --noEmit）通过；旧调用不报错
- [x] 7. 前端 SkillsView 加「MCP servers」section：列 `listMcpServers` 返回的条目，每行 Toggle；点击调 `toggleMcpServer` + `logAction('claude-settings','toggle-mcp', ...)` → verify: 浏览器看到 MCP 区出现 browser-use + codegraph；关 browser-use 后 LogsView 有 `claude-settings toggle-mcp` 起止 + `mcp-toggle` 起止 + 重启 Claude 后 `.mcp.json` 干净
- [x] 8. 前端 SkillsView 全局插件 + 技能行级三档按钮条：根据全局 + 项目级覆盖计算"跟随/强制开/强制关"三态；点击非"跟随"档时调 `patchProjectClaudeSettings`，点"跟随"时删除项目级覆盖 key → verify: 浏览器三档按钮高亮状态正确；webnovel-writer 全局关 + 小说项目"强制开" → 重启该项目 Claude 终端能看到 plugin；其它项目看不到
- [x] 9. 类型检查 + 后端构建：`pnpm --filter @aimon/server build`、`pnpm --filter @aimon/web build` 全绿 → verify: 两个命令退出码 0
- [x] 10. 交付前自派 `vibespace-browser-tester` 跑浏览器验收清单（按 plan 的验收项 A-F） → verify: tester 返回 PASS/FAIL 报告，有 FAIL 项要先修再交付 // **改为后端 e2e curl smoke + 推迟浏览器 UI 验收**：当前 stable server (8787) 跑旧 dist 代码，新路由 404，浏览器端无法跑通；替代地用 temp server (8799) 跑了端到端 curl 测试，全部 PASS（列 MCP / 读项目 settings / toggle / 边界 400-404）。浏览器 UI 验收待大哥重启 VibeSpace 后端进程后做。
