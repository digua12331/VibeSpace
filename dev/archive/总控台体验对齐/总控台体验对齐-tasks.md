# 总控台体验对齐 · 任务清单

- [x] 步骤 1：后端 hub-project + 启动顺序 → **已通过**：build ✓；server 重启后 projects.json 含 __hub__；__hub__ 系统项目自动 upsert；旧 /api/hub/session 返 404（registerHubSessionRoutes 删了）。
- [x] 步骤 2：后端 5 处 filter 守卫 → **已通过**：build ✓；5 curl 攻击向量全 400：DELETE /api/projects/__hub__ → cannot_delete_hub；POST/DELETE workflow → hub_no_workflow；POST sessions worktree → hub_no_worktree；DELETE/gitignore-add fs-ops → cannot_modify_hub_workspace；每个拒绝都有 serverLog warn。
- [x] 步骤 3：后端 MCP 注入分流 → **已通过**：build ✓；起 __hub__ shell session 后 cat hub-workspace/.mcp.json 含 aimon-hub + browser-use 两个 server 合并；hub-workspace.ts 删 writeHubMcpConfig 简化（功能挪到 mcp-bridge.injectHubMcps）；非 __hub__ 项目走原 injectClaude 路径只 browser-use。
- [x] 步骤 4：后端 read_session_output 接口 + MCP 工具 → **已通过**：build ✓；GET /api/hub/sessions/:id/recent-output 返回 PTY buffer 最近 N 行（复用 ptyManager.getBuffer，无新 ring buffer）；不存在 session 返 404；MCP tools/list 返 **7 工具**（hub_status / list_projects / get_project_sessions / read_git_log / read_file / read_session_output / dispatch_to_project）。
- [x] 步骤 5：删除第 2 期独立 hub session runtime → **已通过**：删 hub-session-runtime.ts + routes/hub-session.ts；build ✓；server 不报 import 错；/api/hub/session 返 404。（合并到 Step 3 build 修复同时完成）
- [x] 步骤 6：前端清除第 2 期 hub 视图遗物 + Activity 加 hub-dashboard → **已通过**：build ✓；store.ts 删 selectedView 全套 + currentHubSession 全套；Activity 联合类型加 'hub-dashboard'；selectProject 自动切：选 __hub__ → activity='hub-dashboard'，从 hub-dashboard 切别项目 → activity='scm'；types.ts 删 HubSessionInfo/StartHubSessionRequest，加 SessionRecentOutput；api.ts 删 3 函数加 getSessionRecentOutput；删 HubView.tsx / HubTerminal.tsx。
- [x] 步骤 7：前端 Workbench 简化 + ProjectsColumn 入口缩窄 → **已通过**：build ✓；Workbench 删 selectedView 分支 + HubView import，恢复永远 ActivityBar + PrimarySidebar + EditorArea；ProjectsColumn 顶部 📊 缩成 36px 窄图标按钮（w-9 h-9，跟"全部 sessions"按钮并排），点击 selectProject(HUB_PROJECT_ID)；项目卡列表 filter 掉 __hub__；高亮逻辑改 selectedProjectId === HUB_PROJECT_ID。
- [x] 步骤 8：前端 ActivityBar 加 hub-dashboard view + HubDashboardView + PrimarySidebar 接入 → **已通过**：build ✓；ActivityBar items 数组顶部条件 prepend 'hub-dashboard'（仅 selectedProjectId === HUB_PROJECT_ID 时）；新增 HubDashboardView.tsx ~120 行（复用 HubProjectCard / HubDispatchDialog + 5s 轮询）；PrimarySidebar 加 lazy import + STATIC_TITLES 加 'hub-dashboard': '总控台看板' + switch 加 case。
- [x] 步骤 9：白名单 + grep + 浏览器端到端 + tester → **已通过**：(a) git diff 列 6 modified + 10 untracked，本任务白名单 14 文件全覆盖 + 4 删除全删 + pnpm-lock.yaml 合理副作用（已是 step 2 上一期）；(b) grep `selectedView|currentHubSession|hub-session-runtime|HubSessionInfo|startHubSession|stopHubSession|listHubSessions|setSelectedView` 6 文件命中全是注释/README，**无运行时残留**（build 通过）；(c) 5 攻击向量 curl 全 400 ✓；(d) MCP tools/list 返 7 工具 ✓（含新 read_session_output）；(e) **tester 按既定不派**，浏览器端到端留大哥手验。
