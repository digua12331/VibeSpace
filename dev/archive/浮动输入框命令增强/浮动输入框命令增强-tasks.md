# 浮动输入框命令增强 · 任务清单

- [x] 步骤 1 新建 `packages/web/src/components/terminal/slashCommands.ts`（最终命令表 + `getSlashCommands(agent)` helper；同时在 `dev/issues.md` 追加一条"codex 斜杠命令待补"） → verify: `grep SLASH_COMMANDS packages/web/src/components/terminal/slashCommands.ts` 命中；`grep "codex 斜杠命令" dev/issues.md` 命中一条 `- [ ]`
- [x] 步骤 2 新建 `packages/web/src/components/terminal/InputMenu.tsx`（Portal 渲染、`position: fixed`、anchor 到 input `getBoundingClientRect`、外点关 mousedown 全局监听、items 空时显示"无匹配"、不装键盘监听） → verify: 文件存在；Props 签名对齐 context D1；`pnpm -C packages/web exec tsc -b` SessionView.tsx / InputMenu.tsx / slashCommands.ts 三文件无新增错误
- [x] 步骤 3 SessionView.tsx 集成核心：import、`MenuState` + `useState<MenuState>({kind:'none'})` + `filesRef: useRef<string[]|null>(null)`、`detectTrigger(el)` / `pickSlash(cmd)` / `pickMention(path)` / `fillInput(text)` 四个 helper、`onInput` 事件在 input 上绑定、`onInputKey` 在 menu 开时分支处理 ↑↓/Enter/Tab/Esc（Tab 必须 preventDefault）、menu 关时走原 Enter 发送、active→inactive useEffect 里 `setMenu({kind:'none'})`、惰性调 `api.listProjectFiles` 缓存到 filesRef → verify: diff 含上述所有结构；`tsc -b` 无新增错
- [x] 步骤 4 SessionView.tsx 替换直发为填入：PromptLibraryDialog.onSend 从 `aimonWS.sendInput` 改成 `fillInput(text)` + `setPromptLibOpen(false)`；自定义按钮 onClick 从 `aimonWS.sendInput(cmd + '\r')` 改成 `fillInput(cmd)` → verify: `grep -n "aimonWS.sendInput" packages/web/src/components/terminal/SessionView.tsx` 只剩 `onInputKey` 的 Enter 发送这一处（+ paste image 分支）；PromptLibraryDialog 和 customButtons.map 块里都不再直接调 sendInput
- [x] 步骤 5 SessionView.tsx 渲染 InputMenu：在现有 JSX 底部（`</div>` 前、`confirmClose` dialog 外）条件渲染 InputMenu；items 按 `menu.kind` 从 slashCommands filter 或 `filesRef.current` filter 派生；selectedIndex 从 menu.selected 传入；onPick 分发到 pickSlash/pickMention/关菜单 → verify: diff 含 `<InputMenu ...open={menu.kind !== 'none'} .../>`；浏览器手动打开 `/` 或 `@` 应该看到菜单
- [x] 步骤 6 跑类型检查 → verify: `pnpm -C packages/web exec tsc -b` 无新增 TS 错（DocsView.tsx:372 预先错误仍在，不视为本任务残留）
- [x] 步骤 7 用户浏览器手测验收 → verify: 用户回报以下六项**全过**：(a) claude session 打 `/` 弹 13 条命令、↑↓ Enter Esc 正常、选 `/clear` 填入、再 Enter 才发；(b) 任意位置打 `@` 弹项目文件菜单、输入 `session` 能过滤、选中后插入 `@<path> `（shell 类插入 `"<path>" `）；(c) 点 📝 提示词选一条 → 输入框被覆盖为 prompt 内容、focus 末尾、不发；(d) 点任一自定义按钮 → 输入框被覆盖为命令、不发；(e) shell/cmd/pwsh session 打 `/` 不弹菜单（路径分隔符兼容）；(f) IME composing 中打 `/` 或 `@` 不触发菜单、按 Enter 不误发
