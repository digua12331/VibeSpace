# 手动沉淀（长期经验，大哥写）

> 这份文件由大哥手动维护，不会被自动评审改动。想沉淀一条"换个任务还会用到的"长期经验/约定/偏好，直接往下追加一行即可。
> 建议条目格式与 auto.md 一致：`- [<YYYY-MM-DD> / <来源>] <结论>（上下文：...）`；非单行自然段也可以直接写，前端会以 `raw` 原文展示。

- [2026-04-24 / 大哥偏好] 小功能（量级 ≈"右键菜单加一个条目"、"改一段文案"、"加一个小路由"）直接改，不用走 plan→context→tasks 三段式、不用等确认；AI 自己判断是否算"小功能"，拿不准再问（上下文：2026-04-24 "在浏览器打开 html" 菜单项这次，大哥明确说"以后这种你认为是小功能不需要走流程跟确认直接修改"）
- [2026-04-30 / 大哥偏好] 大哥不懂代码，只关心"大方向 + 能验收"。能拿来给他确认的只有两类：（1）大方向（要解决什么、什么样算解决），（2）有用户感知差异的分叉（A/B 路径会让他最终看到的东西不一样：界面/数据/能否撤销/操作步骤变了）。**纯内部实现**的分叉（用 A 函数 vs B 函数、命名、文件放哪、抽不抽 helper、用哪个库、目录结构）AI 自决，不要拿来让他挑。专业术语第一次出现必须括号解释成白话，并把"做了什么"翻译成"用户看得见的变化"。Dev Docs 三段式因此从"两次确认"压成"一次确认"——只在 plan 后停一次，context 写完直接进 tasks 不停（上下文：2026-04-30 大哥原话"我只需要在大方向把控就可以，且对应的结果能验收就ok，一些技术上的选择你需要基于项目特点自行判断，且我对程序不懂，有些专业术语你需要解释给我听"，本轮已据此重写 CLAUDE.md 的 Plan/Context 段、加"跟大哥说话的规矩"小节、给 plan.md 模板加"大哥摘要"必写段、给 handoff 摘要加"第一行必须是验收指引"约束)
- [2026-04-30 / 大哥偏好] CLAUDE.md Plan 阶段（默认档）改为"三模型协作"默认开，不再靠关键词触发。流程：Claude 调研产出事实包 → 并行调用 Gemini（ask-gemini，长上下文找漏点）+ Codex（codex:rescue，找显著更简结构）各给 ≤30 行评审清单 → 再派一次 Codex 综合三方输入主笔写 plan.md → **Claude 兜底白话化**（重写大哥摘要 / 全文术语括号翻译 / 对照 manual.md+auto.md 偏好——Codex 看不到项目记忆，Claude 必须替它把这些塞回去）→ 呈大哥确认。plan.md 末尾必带 `## 外部审查` 段记录三方原文摘要 + 综合取舍 + 兜底改了哪几处。极小档（改文案 / 加菜单项）和小档（用户说"按你想法做"）**不**走多模型，省外部调用。外部工具失败重试一次仍失败则回退 Claude 单写 + plan.md 记一行原因，**不阻塞 plan 交付**（上下文：2026-04-30 大哥原话"plan阶段可以直接默认就是让claude，gemini跟codex都介入去出一份plan，然后codex去出plan"，对应改动在 CLAUDE.md `### Plan 阶段默认流程：三模型协作`）
- [2026-05-06 / 大哥偏好] 完成 task 时若涉及"可在浏览器观察的验收项"（UI 改动 / 新 mutation API / 新交互），交付前 AI **自己**先派测试 agent（VibeSpace 用 `vibespace-browser-tester`，靠 browser-use MCP 真点真测）跑一遍验收清单，**有问题再汇总给大哥**——不要把"等会儿你自己去浏览器看一眼吧"留给大哥。Agent 跑前要么 dev server 已在跑、要么 AI 自己起（启动方式参见 README/start.bat）。极小档（改文案 / 改一个标题）可以跳，但要在 handoff 里明说"未做浏览器验收，原因是改动可视风险极小"。**默认是跑，不是问**（上下文：2026-05-06 大哥原话"我需要你在完成task时，如有测试任务你需要先自己让测试agent去跑一遍，如果有问题最终汇总给我"，触发原因是"移除 Dev Docs 启动按钮"那次我交付前没自己验，反过来问大哥要不要派 tester）
- [2026-06-02 / 批量派工秒失败修复] 给一个跑 TUI（claude/codex 等占满屏的终端程序）的 PTY 注入 prompt 后，**不要靠扫终端 output buffer 找你自己写进 prompt 的 sentinel 字符串**来判完成/卡住——TUI 会把注入的 prompt 原样回显，buffer 里立刻就有那个 sentinel，瞬间自触发（本 bug 就是 1 秒内误判 stuck，reason 截成占位符 `<一句话原因>` + 残留 ANSI 码）；ANSI、全屏重绘、AI 复述指令也都会再次命中。正确做法：用**带外信号**——让 agent 往一个 worktree 内 gitignored 文件（如 `.aimon/runtime/job-signal`）写 `DONE` / `STUCK: 原因`，server 端 `setInterval + readFileSync` 轮询（别用 fs.watch，Windows/worktree 易丢事件），只认严格前缀避半写竞态，PTY exit 前再补读一次防"写完即退出"竞态（上下文：worktree-session-runner.ts 的 spawnWorktreeJob 是 issue 派工 + 子任务派工共用核心；Codex 会审同结论，并否决了 git commit message / 退出码 / STATUS.md 这几种传信号方式）
- [2026-06-02 / browser-use按需注入] browser-use MCP 改为"默认不注入、项目级手动开（opt-in）"后，没在 MCP 面板开过 browser-use 的项目里，AI 自动派 vibespace-browser-tester 会拿不到 `mcp__browser-use__*` 工具。约定 AI 的行为：**不许静默跳过浏览器验收**——要么交付前先把 browser-use 开起来（API: `PUT /api/mcp-servers/toggle {enabled:true,name:"browser-use"}` 写 .mcp.json，需新开一个会话才生效），要么在 handoff 里**明确写**"本项目未开 browser-use，已跳过自动浏览器验收，原因 X"。这与 2026-05-06"默认是跑不是问"偏好叠加：能开就开了再跑，开不了就显式告知，别假装跑过（上下文：mcp-bridge.ts 不再自动注入 browser-use，真源改为 <project>/.mcp.json 是否含该条目；toggle ON 经 writeBrowserUseToMcpJson 主动写入）
