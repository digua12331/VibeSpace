# 手动沉淀（长期经验，主理人写）

> 这份文件由主理人手动维护，不会被自动评审改动。想沉淀一条"换个任务还会用到的"长期经验/约定/偏好，直接往下追加一行即可。
> 建议条目格式与 auto.md 一致：`- [<YYYY-MM-DD> / <来源>] <结论>（上下文：...）`；非单行自然段也可以直接写，前端会以 `raw` 原文展示。

- [2026-04-24 / 主理人偏好] 小功能（量级 ≈"右键菜单加一个条目"、"改一段文案"、"加一个小路由"）直接改，不用走 plan→context→tasks 三段式、不用等确认；AI 自己判断是否算"小功能"，拿不准再问（上下文：2026-04-24 "在浏览器打开 html" 菜单项这次，主理人明确说"以后这种你认为是小功能不需要走流程跟确认直接修改"）
- [2026-04-30 / 主理人偏好] 主理人不懂代码，只关心"大方向 + 能验收"。能拿来给他确认的只有两类：（1）大方向（要解决什么、什么样算解决），（2）有用户感知差异的分叉（A/B 路径会让他最终看到的东西不一样：界面/数据/能否撤销/操作步骤变了）。**纯内部实现**的分叉（用 A 函数 vs B 函数、命名、文件放哪、抽不抽 helper、用哪个库、目录结构）AI 自决，不要拿来让他挑。专业术语第一次出现必须括号解释成白话，并把"做了什么"翻译成"用户看得见的变化"。Dev Docs 三段式因此从"两次确认"压成"一次确认"——只在 plan 后停一次，context 写完直接进 tasks 不停（上下文：2026-04-30 主理人原话"我只需要在大方向把控就可以，且对应的结果能验收就ok，一些技术上的选择你需要基于项目特点自行判断，且我对程序不懂，有些专业术语你需要解释给我听"，本轮已据此重写 CLAUDE.md 的 Plan/Context 段、加"跟主理人说话的规矩"小节、给 plan.md 模板加"主理人摘要"必写段、给 handoff 摘要加"第一行必须是验收指引"约束)
- [2026-04-30 / 主理人偏好] CLAUDE.md Plan 阶段（默认档）改为"三模型协作"默认开，不再靠关键词触发。流程：Claude 调研产出事实包 → 并行调用 Gemini（ask-gemini，长上下文找漏点）+ Codex（codex:rescue，找显著更简结构）各给 ≤30 行评审清单 → 再派一次 Codex 综合三方输入主笔写 plan.md → **Claude 兜底白话化**（重写主理人摘要 / 全文术语括号翻译 / 对照 manual.md+auto.md 偏好——Codex 看不到项目记忆，Claude 必须替它把这些塞回去）→ 呈主理人确认。plan.md 末尾必带 `## 外部审查` 段记录三方原文摘要 + 综合取舍 + 兜底改了哪几处。极小档（改文案 / 加菜单项）和小档（用户说"按你想法做"）**不**走多模型，省外部调用。外部工具失败重试一次仍失败则回退 Claude 单写 + plan.md 记一行原因，**不阻塞 plan 交付**（上下文：2026-04-30 主理人原话"plan阶段可以直接默认就是让claude，gemini跟codex都介入去出一份plan，然后codex去出plan"，对应改动在 CLAUDE.md `### Plan 阶段默认流程：三模型协作`）
