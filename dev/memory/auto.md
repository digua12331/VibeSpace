# 自动沉淀（归档评审产出）

> 这份文件由"归档评审"自动追加。归档任一任务时后端会调 codex/gemini 评审归档目录，把"换个任务还会再用到"的经验按单行格式追加到下方。
> 条目格式：`- [<YYYY-MM-DD> / <任务名>] <结论>（上下文：<为什么会重复踩>）`。不要手动改这里的条目——UI「记忆」tab 勾选+撤回即可，撤回的条目会移到 `rejected.md`。
- [2026-04-23 / hook-smoke-1776939102315] 经验 A（上下文：hook 冒烟）
- [2026-05-01 / 修复开发启动器依赖缺失] Windows 专用启动入口若只是给子进程注入环境变量，优先在 `.bat` 里直接 `set` 后再调用底层命令，避免平白增加 `cross-env` 这类中间依赖（上下文：这个仓库长期保留 `start.bat` 作为主入口，后续再加开发/调试开关时都可能重复踩到同类脆弱链路）
- [2026-05-01 / 修复开发启动器依赖缺失] 排查 `Cannot find module` 一类启动故障时，先对照 `pnpm-lock.yaml` 与 `node_modules/.pnpm` 的实际链接结构，先区分“声明缺失”还是“本地安装目录残缺”再决定修法（上下文：这个仓库依赖 pnpm 的链接式安装，后续任何依赖异常都可能再次出现“锁文件正常但本地链接坏了”的情况）
- [2026-05-01 / 更新README] 新增用户可见功能或接口时，要同步更新 `README.md` 与 `README.zh-CN.md` 的功能列表、架构说明和 API 清单，不能只补一个入口（上下文：这个仓库长期维护双语主文档，后续功能迭代会反复遇到文档漂移）
- [2026-05-01 / 更新README] 面向大哥的说明要优先写“在哪里点、会看到什么、会不会动到现有数据/界面”，并把第一次出现的专业术语顺手翻成白话（上下文：仓库已经明确按非程序员可验收来写计划、交付和说明，后续所有文档类任务都会重复用到）
- [2026-05-01 / 更新README] 涉及 Dev Docs、记忆、Issues、Skills 这类流程能力时，文档必须同时写清楚磁盘路径、触发入口和生命周期，而不是只写概念名（上下文：这类能力横跨 UI、服务端和仓库约定文件，未来继续扩展时最容易因为落点没写全而不可用）
- [2026-05-01 / 更新README] 项目级规则一旦同时出现在 `README`、`AGENTS.md`、`CLAUDE.md` 或 `.aimon/docs`，改一处时就要把同类入口一起核对一遍（上下文：这个仓库用多份文档面向不同读者，同一工作流会持续在多处重复出现）
- [2026-05-01 / 更新README] README 维护应以代码现状做增量校对和补漏为主，不为“更整齐”重写整份说明（上下文：项目功能面广且迭代快，未来更常见的是修正文档漂移而不是推倒重来）
- [2026-05-01 / 终端背景主题评估] 终端视觉主题同时受 xterm 组件主题和全局 CSS 影响，后续改终端背景、文字、光标或选中样式时要同时检查这两层来源（上下文：只改其中一处容易出现浏览器里看起来没生效或新旧样式混用）
- [2026-05-01 / 终端背景主题评估] “每个终端独立配置”这类需求通常不只是样式改动，还会牵涉 session（终端实例的数据结构）持久化和恢复逻辑（上下文：未来遇到按终端保存偏好、状态或主题的需求时，需要先判断是否要扩展会话数据）
- [2026-05-01 / 项目工作流统一装配] 合并或删除后端 API 前必须全仓库搜索所有调用点，不能只相信 plan 里写的“唯一消费者”（上下文：DocsView 顶部按钮仍调用旧 Dev Docs API，执行阶段才发现并补边界，这类遗漏在任何端点收敛任务都会重复）
- [2026-05-01 / 项目工作流统一装配] “安装/卸载一整套项目配置”这类功能不要只做二态状态，历史项目很容易出现部分已应用，接口和 UI 都要表达 partial（上下文：Dev Docs 与 Harness 曾经可单独安装，合并后必须兼容旧项目的半安装状态）
- [2026-05-01 / 项目工作流统一装配] 搬迁会被模板分发的文档或配置文件时，要同步检查源文件路径、模板 manifest、卸载清单、gitignore 白名单和文档内互链（上下文：这些位置共同决定“源码能提交、应用能拷贝、卸载能清理、说明能点通”，后续搬文件任务会反复遇到）
- [2026-05-01 / 项目工作流统一装配] 前端 logAction 和后端 serverLog 同时记录同一用户操作时，plan/context 里要明确外层用户动作日志与内层服务日志的关系，避免验收时把重复日志误判成问题（上下文：apply-workflow/remove-workflow 会在 LogsView 同时出现前端与后端起止配对）
- [2026-05-01 / 项目工作流统一装配] 如果验收项需要人工浏览器操作或故意制造失败分支，tasks 里应把它明确标成“待大哥手动验收”，不要和自动类型检查混成已完成交付（上下文：本任务类型检查已过，但浏览器实操和锁文件失败分支仍需人工触发）
- [2026-05-01 / Harness入口统一到抽屉] 删除旧入口时要同步检查并迁移旧入口独有的成功反馈，避免用户可见能力随入口合并而退化（上下文：后续合并抽屉、菜单、页面入口时都会重复遇到“入口删了但提示/详情丢了”的风险）
- [2026-05-01 / Harness入口统一到抽屉] 前端删除消费方后，后端无人调用的路由如果不影响当前验收，应先记录到 `dev/issues.md`，不要顺手扩大成跨前后端清理（上下文：本仓库常有前端入口和后端服务解耦，范围控制能减少非必要改动）
- [2026-05-01 / Harness入口统一到抽屉] 只剩单一调用点的展示格式不要抽公共 helper，直接贴近调用处实现更符合本仓库外科式改动原则（上下文：小型 UI 行为合并任务里，过早抽象会增加阅读成本且缺少复用收益）
- [2026-05-01 / 工作流入口形态对齐] 做“卸载/撤销”类功能时，状态探测不要只看目录是否存在，应检测代表性文件或清理空目录，否则卸载后重新打开界面可能回弹为“已应用”（上下文：本仓库多处 apply 会创建目录，unlink 文件后空目录残留会影响后续状态判断）
- [2026-05-01 / 工作流入口形态对齐] 对模板类 apply/uninstall 应优先复用同一份 manifest（文件清单）并按文件逐个处理，避免目录级删除误伤用户后来新增的文件（上下文：Harness、Dev Docs 等模板能力都会往项目目录写入成组文件，未来新增类似功能会重复遇到卸载边界）
- [2026-05-01 / 工作流入口形态对齐] 前后端同一用户操作的日志 scope/action 要保持一致，新增或调整 mutation（会改状态的接口）时同时核对 LogsView 和后端落盘日志（上下文：本任务发现 apply-harness 前端用 project、后端用 installer，类似不一致会让排障时日志串不起来）
- [2026-05-01 / 工作流入口形态对齐] Fastify 返回 207 仍属于 2xx，前端 `request()` 不会抛错；需要把“部分失败”作为正常返回值分支处理，而不是只写 catch（上下文：未来文件批处理、批量删除、批量同步类接口可能继续用 207 表达部分成功）
- [2026-05-01 / 项目列表激活分页] 判断 session 是否“运行中”要以 `ended_at == null` 为准，不能只看 `sessions` 数组里有没有这条记录（上下文：`markSessionExit` 会保留已退出 session，后续涉及激活状态、数量徽标、列表分组都容易复用这个判断）
- [2026-05-01 / 项目列表激活分页] 纯前端视图切换不触发服务端状态变化时，不需要额外埋操作日志（上下文：tab、筛选、展开收起这类本地 UI 状态后续会反复出现，避免日志噪声）
- [2026-05-01 / 新建项目极简化与设置整合] 文件里已有锚点块的开关状态优先从文件内容实时读取，不要另加数据库字段制造双份状态（上下文：Dev Docs 这类工作流配置以后还会继续以 CLAUDE.md/AGENTS.md 片段形式存在，实时读能减少迁移和同步问题）
- [2026-05-01 / 新建项目极简化与设置整合] 删除自动追加的文档块时要做成幂等操作，并在可能删到用户自写内容时加确认弹窗（上下文：后续任何“关闭某工作流/移除模板段落”的功能都会遇到锚点不存在、重复关闭和误删尾部内容的问题）
- [2026-05-01 / 新建项目极简化与设置整合] PermissionsDrawer 这类已有大组件新增小 tab 时先跟随现有内联组件风格，不为一次性面板单独抽文件（上下文：该抽屉已有 ClaudeTab/CodexTab/ButtonsTab 内联，未来加设置页应优先保持局部一致性）
- [2026-05-01 / 新建项目极简化与设置整合] 用户可感知的项目操作要前端 logAction 与后端 serverLog 同时覆盖，并把失败分支写进验收清单（上下文：项目创建、工作流开关、文件写入这类操作后续排障都依赖 LogsView 和落盘日志回放）
- [2026-05-02 / 工作流方法论提取为skill] 抽取全局 skill 时只搬通用方法论，项目专属路径、函数名、后台机制继续留在项目规则里（上下文：以后再做跨项目复用时，容易把 VibeSpace 专属实现误塞进全局提示）
- [2026-05-02 / 工作流方法论提取为skill] 迁移工作流规则适合采用“双轨并存”：先新增复用入口，不立刻删除原项目强制规则（上下文：以后改动基础工作流时，可降低触发不稳定或漏加载带来的风险）
- [2026-05-02 / 工作流方法论提取为skill] Claude skill 的 `description` 要覆盖用户真实会说的触发词，而不是只写内部术语（上下文：以后创建或调整 skill 时，加载是否稳定主要取决于描述命中率）
- [2026-05-02 / 技能管理面板] 项目相关后端接口应优先走 `/api/projects/:id/...` 并由服务端按项目 id 反查路径，不让前端直接传项目路径（上下文：后续任何会读写项目文件的功能都复用这个安全边界） [category=约定; severity=warn; files=packages/server/src/routes/skill-catalog.ts]
- [2026-05-02 / 技能管理面板] 会删除、复制、移动本地目录的接口必须同时校验用户输入名和 `path.resolve()` 后的最终路径归属，避免路径穿越误删项目外文件（上下文：后续文件管理、安装器、清理类功能都会重复遇到这个风险） [category=踩坑; severity=error; files=packages/server/src/skill-catalog-service.ts]
- [2026-05-02 / 技能管理面板] 会改文件的后端操作要用 `serverLog` 记录开始、成功、失败三类日志，前端触发的写操作要用 `logAction` 包起止配对（上下文：后续用户可感知操作都需要能在 LogsView 和落盘日志里回放） [category=操作流程; severity=warn; files=packages/server/src/routes/skill-catalog.ts,packages/server/src/log-bus.ts]
- [2026-05-02 / 技能管理面板] Windows 上默认不要依赖 symlink（符号链接，像快捷方式一样指向目录），安装类功能优先复制，链接只作为高级选项（上下文：后续插件、模板、skill 安装都会碰到 Windows 权限导致链接失败的问题） [category=兼容性; severity=warn; files=packages/server/src/skill-catalog-service.ts]
- [2026-05-02 / 技能管理面板] README 和界面文案里要明确区分 VibeSpace 内部 `.aimon/skills` 与各 AI CLI 自己读取的 `.claude/.codex/.opencode/skills`（上下文：后续凡是提到 skill、hook、CLI 配置的任务都容易把两套机制混在一起） [category=约定; severity=info; files=README.md,README.zh-CN.md]
- [2026-05-02 / 使用量面板] 后端是 NodeNext ESM，相对 import 必须带 `.js` 后缀，否则类型检查或运行时会失败（上下文：以后新增 server 路由、服务文件时都会重复遇到这个模块解析约定） [category=工具链; severity=warn; files=packages/server/src/index.ts]
- [2026-05-02 / 使用量面板] 用户可触发的前后端读写操作要同时做前端 `logAction` 和后端 `serverLog` 起止配对，且失败分支也要能在 LogsView 看到 ERROR（上下文：这是本仓库排障和验收的通用路径，不只适用于使用量面板） [category=操作流程; severity=warn; files=packages/server/src/log-bus.ts]
- [2026-05-02 / 使用量面板] 日志 meta 只放小标量，不要塞完整聚合结果或大对象，保持 JSON 可序列化且不超过 2KB（上下文：后续任何新日志都会落盘并广播到 LogsView，过大的 meta 会影响可读性和稳定性） [category=约定; severity=warn; files=packages/server/src/log-bus.ts]
- [2026-05-02 / 使用量面板] 读取用户家目录下的工具数据时必须用 `os.homedir()` + `path.join`，不要硬编码 Windows 路径或斜杠（上下文：后续接入本地 CLI、配置目录、日志目录时都需要跨平台路径处理） [category=兼容性; severity=warn; files=packages/server/src]
- [2026-05-02 / 修复自定义CSS不生效] 主题相关逻辑如果同时存在运行时注入和 `index.html` 首屏兜底脚本，两处规则必须同步维护，否则刷新前后会出现不一致体验（上下文：主题、外观、首屏防闪烁类任务以后还会反复遇到） [category=踩坑; severity=warn; files=packages/web/src/theme/store.ts,packages/web/index.html]
- [2026-05-02 / 修复自定义CSS不生效] 自定义 CSS 要覆盖预设主题时，不能只依赖 `<style>` 插入顺序，还要检查选择器优先级（上下文：后续任何“用户配置不生效/被主题覆盖”的问题都可能是 CSS 权重导致） [category=踩坑; severity=warn; files=packages/web/src/theme/themes.css,packages/web/src/theme/tokens.css]
- [2026-05-02 / 修复自定义CSS不生效] Web 包缺少单测基建时，小型前端修复不要为了一个点临时搭测试框架，优先用浏览器可见验收加项目级类型检查兜底（上下文：外科式 UI 修复常见，避免把小 bug 扩成测试基础设施任务） [category=测试; severity=info; files=packages/web]
- [2026-05-02 / 接入-browser-use] 对“能安装但不能作为会话启动”的外部工具，应在 CLI catalog 里显式区分 agent 与 mcp-tool，避免它误进启动下拉菜单（上下文：后续接入验证器、扫描器、MCP 工具时都会重复遇到“可安装但不可启动”的分类问题） [category=约定; severity=warn; files=packages/server/src/cli-catalog.ts,packages/web/src/components/StartSessionMenu.tsx]
- [2026-05-02 / 接入-browser-use] 项目级 MCP 配置优先写项目根而不是 worktree 或全局配置，写入必须幂等并保留已有 server 条目（上下文：多项目、多 worktree、重复启动 session 都会反复触发配置注入，写错位置或重复写会污染环境） [category=兼容性; severity=error]
- [2026-05-02 / 接入-browser-use] 外部工具链首次运行可能隐式下载大依赖，installer UI 和 README 都要把前置依赖、耗时和失败提示讲清楚（上下文：以后接入 Python、Playwright、模型 CLI、浏览器工具时都可能出现“看起来卡住其实在下载”的体验问题） [category=工具链; severity=warn; files=README.md,README.zh-CN.md]
- [2026-05-02 / 接入-browser-use] 可选增强能力注入失败时不要阻塞主 session 启动，但必须用操作日志记录起止、耗时、失败原因和关键 meta（上下文：后续任何插件、MCP、hook、验证器注入都应保持主流程可用，同时让 LogsView 能追溯问题） [category=操作流程; severity=warn; files=packages/server/src/log-bus.ts]
- [2026-05-02 / 运行 Python 文件] 需要“点一下立即在终端执行”的文件动作时，优先复用现有 `createSession` + `aimonWS.subscribe` + 120ms 等待 + `aimonWS.sendInput` 模式，而不是走 `sendToSession`，因为后者会排队等用户再按 Enter（上下文：后续任何一键运行/执行类入口都会遇到同样的“立即发命令还是只预填命令”差异） [category=约定; severity=warn; files=packages/web/src/components/fileContextMenu.ts,packages/web/src/components/sendToSession.ts]
- [2026-05-02 / 运行 Python 文件] 前端新增用户主动触发的执行类动作时，用 `logAction(scope, action, fn, ctx)` 包住异步主流程，让 LogsView 自动得到开始/成功/失败配对日志（上下文：操作日志是本仓库验收与排障的固定入口，后续 mutation 类前端动作都要保持同一形态） [category=约定; severity=warn; files=packages/web/src/logs.ts]
- [2026-05-02 / 运行 Python 文件] FilePreview 打开的历史快照不等于磁盘上的当前文件，任何“运行/执行当前文件”的入口都要明确它实际运行的是 worktree 当前版本，必要时用 title 或文案提示（上下文：后续对预览文件做运行、删除、改名等磁盘动作时都会有 ref 与 worktree 语义分离的问题） [category=踩坑; severity=warn; files=packages/web/src/components/FilePreview.tsx]
- [2026-05-02 / 项目切换卡顿优化] 优化用户感知卡顿时先加可见埋点和 LogsView 日志，再改性能热点，否则只能凭体感判断效果（上下文：切项目耗时、首切/复切、降级触发这类优化以后还会反复需要可量化验收） [category=性能; severity=info; files=packages/web/src/perf-marks.ts,packages/web/src/logs.ts]
- [2026-05-02 / 项目切换卡顿优化] xterm/IME/TUI 这类复杂交互组件优先通过稳定挂载层和显隐控制保活，不轻易改组件内部生命周期（上下文：SessionView 内部强耦合，未来类似终端或编辑器性能任务也应先避免卸载重建） [category=约定; severity=warn; files=packages/web/src/components/terminal/SessionView.tsx,packages/web/src/components/editor/EditorArea.tsx]
- [2026-05-02 / 项目切换卡顿优化] 浏览器内存降级只能把 `performance.memory.usedJSHeapSize` 当 Chromium 兜底信号，非 Chromium 要按 unavailable 处理且避免自动来回切策略（上下文：前端保活、缓存、长会话任务以后还会遇到内存阈值和体感稳定性的取舍） [category=兼容性; severity=warn; files=packages/web/src/perf-marks.ts]
- [2026-05-02 / 项目切换卡顿优化] 小范围 stale-while-revalidate 缓存直接放现有 store，配一个“刷新中”可见状态即可，不为单点缓存引入新查询库（上下文：ChangesList/GitGraph 这种项目级轻量数据缓存以后还会出现，过度引库会扩大维护面） [category=约定; severity=info; files=packages/web/src/store.ts,packages/web/src/components/ChangesList.tsx,packages/web/src/components/GitGraph.tsx]
- [2026-05-02 / 项目切换卡顿优化] Dev Docs 的关键文件边界要在收尾时对照实际 changed files，若出现大量边界外文件必须单独解释或拆任务（上下文：本任务计划只动 web 性能路径，但完成列表包含 server、README、memory 等多类文件，未来评审容易发现任务外扩散） [category=操作流程; severity=warn]
- [2026-05-02 / 会话启动模板-yaml化] 涉及 session 启动、首帧输入或终端写入时，先盘清后端 PTY 通道和前端输入区的实际数据流，不要假设“启动后塞一段文本”有现成落点（上下文：模板、skill 注入、worktree 启动这类任务都会复用同一条 session 启动链路） [category=踩坑; severity=warn; files=packages/web/src/components/editor/EditorArea.tsx]
- [2026-05-02 / 会话启动模板-yaml化] 项目级可选配置目录应按“目录不存在等于功能隐藏、单个坏文件只跳过并记日志、整体接口仍成功返回”处理（上下文：templates、skills、market、memory 等项目级文件目录都需要避免一个坏配置拖垮主界面） [category=约定; severity=warn]
- [2026-05-02 / 会话启动模板-yaml化] 新增面向用户的可配置能力时，要同时交付 schema 文档、示例文件和坏输入验收用例，不能只加读取代码（上下文：后续 YAML/JSON/Markdown 配置入口都会遇到“用户手写配置出错怎么排查”的问题） [category=测试; severity=info]
- [2026-05-02 / 会话启动模板-yaml化] Dev Docs 任务不能只留 plan，context.md 和 tasks.md 也必须补齐，否则归档评审无法判断真实改动边界、决策依据和验证结果（上下文：跨文件任务完成后经常需要靠这三份文档衔接记忆和复盘） [category=操作流程; severity=warn]
- [2026-05-02 / 会话启动模板-yaml化] 碰到已有未提交改动的核心文件时，先确认这些改动是否属于当前任务边界，再决定继续、拆阶段或暂停（上下文：session、editor、api、types 这类共享文件常被多个任务连续修改，直接叠改容易制造难以归因的回归） [category=操作流程; severity=warn]
- [2026-05-02 / 右键菜单发送到输入框] 跨组件把文字塞进会话输入框时，优先用 store 保存“待注入文本”这类瞬时状态，不要持久化到 localStorage，刷新后丢失才符合用户预期（上下文：后续任何菜单、按钮、侧栏入口想把内容填到 SessionView 输入框，都会遇到同样的跨组件通信和刷新语义问题） [category=约定; severity=info; files=packages/web/src/store.ts,packages/web/src/components/terminal/SessionView.tsx]
- [2026-05-02 / 右键菜单发送到输入框] “发送到已有 session 输入框”的公共路径应复用 `sendToSession`，保持切 tab、激活 session、注入输入框、操作日志这一组行为一致（上下文：文件右键、Dev Docs 派发、未来其它入口都可能做同类派发，复制实现容易漏日志或漏切换 tab） [category=约定; severity=warn; files=packages/web/src/sendToSession.ts]
- [2026-05-02 / 右键菜单发送到输入框] 对用户可见的派发操作必须用 `logAction` 包起止日志，并用不同 scope 区分来源，例如 `files` 和 `docs`（上下文：后续排查需要在 LogsView 里分辨到底是哪个入口触发，少了起止配对会让验收和排障都失真） [category=测试; severity=warn; files=packages/web/src/logs.ts]
- [2026-05-02 / 右键菜单发送到输入框] 新开 session 后立刻注入输入框存在挂载时机风险；没有存活目标 session 时，优先保留既有 fallback 流程，避免为了统一路径引入不稳定异步等待（上下文：未来涉及启动 CLI、创建终端、再填输入框的功能都会遇到 SessionView 尚未就绪的问题） [category=踩坑; severity=warn; files=packages/web/src/dispatchClaude.ts,packages/web/src/sendToSession.ts]
- [2026-05-02 / 终端方向键直通PTY] `SessionView` 的 xterm 维持 `disableStdin: true` 时，任何想让终端内 TUI 收到的非打印键都必须显式映射成 ANSI 序列并走 `aimonWS.sendInput`，否则会被自定义键盘处理器屏蔽（上下文：后续凡是改终端键盘交互、菜单、prompt、PTY 输入都容易再次踩到这条路径） [category=踩坑; severity=error; files=packages/web/src/components/terminal/SessionView.tsx]
- [2026-05-02 / 终端方向键直通PTY] 终端键盘透传的守卫顺序应优先保护 IME 组合输入、textarea 焦点、textarea 非空状态，再判断白名单按键（上下文：后续新增快捷键或 TUI 按键时，错把输入法或悬浮输入框按键发进 PTY 会直接破坏用户打字） [category=兼容性; severity=warn; files=packages/web/src/components/terminal/SessionView.tsx]
- [2026-05-02 / 终端方向键直通PTY] 高频键盘事件不要用逐次操作日志或 `logAction` 起止配对，应该只在关键状态首次触发时用幂等日志标记一次（上下文：LogsView 只保留有限内存日志，后续键盘、拖拽、轮询类功能逐条打日志会淹没有用信息） [category=约定; severity=warn; files=packages/web/src/logs.ts,packages/web/src/components/terminal/SessionView.tsx]
- [2026-05-02 / 终端方向键直通PTY] `packages/web` 没有单独 `typecheck` 脚本时，用 `pnpm -F @aimon/web build` 作为类型检查与前端构建验收命令（上下文：后续 Web 侧源码改动仍需要项目级 TypeScript 校验，不能只靠人工看或局部运行） [category=测试; severity=info; files=packages/web/package.json]
- [2026-05-02 / 技能市场二期] 接收外部仓库地址并交给子进程前，必须先白名单解析再重组安全 URL，不能把用户原始输入直接传给执行命令（上下文：后续任何下载、clone、导入类功能都会重复遇到命令注入风险） [category=安全; severity=error; files=packages/server/src/skill-market-service.ts]
- [2026-05-02 / 技能市场二期] 会写本机文件或配置的用户可见操作，要同时补前端 logAction 和后端 serverLog，并验收 LogsView 与落盘 JSONL 的起止配对（上下文：后续新增 mutation 接口都需要可回放、可排障） [category=操作流程; severity=warn; files=packages/server/src/routes/skill-market.ts,packages/server/src/log-bus.ts]
- [2026-05-02 / 技能市场二期] 全机器级能力优先挂独立 `/api/<feature>/*` 路由，不要硬塞进 `/api/projects/:id/*`（上下文：后续类似市场、全局配置、本地库这类功能都不属于单个项目） [category=约定; severity=info; files=packages/server/src/routes/skill-market.ts,packages/server/src/index.ts]
- [2026-05-02 / 技能市场二期] 人工浏览器验收和失败分支验收没跑完时，tasks 里必须保持未完成并写清“待主理人触发”，不要把 typecheck 通过冒充完整验收（上下文：后续 UI 或联网功能都会有自动测试覆盖不到的人工路径） [category=测试; severity=warn; files=dev/active/技能市场二期/技能市场二期-tasks.md]
- [2026-05-02 / 技能市场二期] 面向用户的技能列表适合按短横线前缀自动分组，并在组级提供批量安装或卸载，项目技能、全局技能、本地库三栏应复用同一分组逻辑（上下文：后续技能管理 UI 继续扩展时应保持三栏行为一致） [category=约定; severity=info; files=packages/web/src/components/sidebar/SkillsView.tsx]
