# 团队模板借鉴鲁班四条 · Plan

> memory 扫过：auto.md/manual.md 无与本次直接相关条目；上一任务（工作流装配agent团队）的指纹升级机制是本次的分发前提。

## 大哥摘要

把鲁班 skill 里验证过的四个做法搬进我们的 agent 团队：(1) **模板回写约定**——任何项目里发现团队成员的通用毛病，最终改 VibeSpace 的母版、靠升级机制铺到所有项目，让团队"全网进化"；(2) **派工填空模板**——团队说明书里给主 AI 两个现成模板（派实施工、派验收员），照着填就是合格派工；(3) **经理 AI 加授权细则**——"疑问句不算授权，祈使句才算；新项目首单只派小卡"；(4) **子工卡死心跳**——说明书补"子工长时间没动静怎么办"。全是改文档/模板，验收：重新应用工作流后，各项目的团队说明书自动刷新出这些内容。

## 目标

- team-usage.md 母版新增：派工填空模板 ×2、模板回写上报约定、子工心跳条目 → 改后 `pnpm smoke:agent-team` 仍全过（指纹机制使旧装项目重新应用时自动刷新）。
- 经理AI受约束派工.md 铁律区加"疑问句≠授权"细则 + 标准流程加首单小卡信任阶梯。
- dev/agent-team-blueprint.md 加"母版回写"一节（VibeSpace 仓库侧的责任约定）。

## 非目标

- 不写 examples 实战案例（等真实跑单后沉淀）；不动 4 个角色模板正文；不动任何代码。

## 实施步骤

1. team-usage.md 加三段 → verify: smoke:agent-team 全过（含禁词断言）。
2. 经理AI受约束派工.md 加两条 → verify: 通读无与现有铁律冲突。
3. blueprint 加母版回写节 → verify: 内容与 team-usage 上报路径互相呼应。

## 边界情况 / 风险与注意

- team-usage.md 改动会使母版指纹变化——已装且未改造的项目重新应用时刷新，这正是预期行为；改造过的项目不受影响（保护用户本地化）。
- 经理 skill 文件会随 harness 拷给其他项目（已知错拷遗留），本次只改内容不动分发。

## 多模型 Plan 会审

跳过：小档（纯文档/模板改动，三文件，易回滚），不调外部模型。
