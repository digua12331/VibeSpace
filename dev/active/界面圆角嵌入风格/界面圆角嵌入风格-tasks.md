# 界面圆角嵌入风格 · 任务清单

- [x] 步骤 1：`index.css` 给 `#root` 加 padding + 新增 `.app-frame` 规则 → verify: 规则含圆角/边框/阴影/overflow
- [x] 步骤 2：`Workbench.tsx` 根 div 加 `app-frame` class → verify: className 含 app-frame
- [x] 步骤 3：web 类型检查 → verify: `tsc -b` 退出码 0
